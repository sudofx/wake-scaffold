(() => {
  'use strict';
  const d=JSON.parse(document.getElementById('wake-data').textContent),s=d.state;
  if(s.charter){document.querySelector('.wordmark').href='#home';document.querySelector('.footer-mark').href='#home';}
  const help=key=>window.WakeHelp.button(key);
  const esc=v=>String(v??'').replace(/[&<>"']/g,x=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[x]));
  const displayName=String(s.pet_name||'WAKE✳︎').replaceAll('\uFE0E','').replaceAll('\u2733','\u2733\uFE0E');
  const names={quantum_physics:'Quantum physics',philosophy:'Philosophy',psychology:'Psychology',ai:'Artificial intelligence',intersections:'Across disciplines'};
  const projects=Object.values(s.projects||{}),books=Object.values(s.notebooks||{}).sort((a,b)=>b.updated_version-a.updated_version);
  const active=projects.filter(p=>p.status==='active');
  const invocations=Object.values(s.invocations),last=invocations.at(-1);
  const format=t=>new Date(t).toLocaleString('en-US',{timeZone:'America/Los_Angeles',month:'short',day:'numeric',hour:'numeric',minute:'2-digit',timeZoneName:'short'});
  const key='wake-visit-'+(d.events[0]?.hash||'new');
  let previous=null;try{previous=JSON.parse(localStorage.getItem(key));localStorage.setItem(key,JSON.stringify({cycle:s.version,time:Date.now()}));}catch{}
  const old=previous?.cycle??s.version;
  const paused=d.operation&&d.operation.status!=='accepted';
  const stale=last&&Date.now()-new Date(last.time).getTime()>12*3600000;
  const status=paused?'Last wake couldn’t finish':stale?'No recent wake':last?.status==='pending'?'A wake was interrupted':['failed','rejected'].includes(last?.status)?'Last wake had trouble':'Between wakes';
  const problem=d.operation?.reason||'The latest wake did not finish. Its record has been preserved.';
  const summarizeProblem=reason=>{
    const text=String(reason||'');
    if(text.includes('HTTP 503'))return 'Gemini stayed busy after a delayed retry. WAKE✳︎ kept its memory and will try again at the next eligible wake.';
    if(/\bfields must be exactly:/i.test(text))return 'The latest proposal did not match WAKE✳︎’s required action shape, so governance rejected it without changing durable state.';
    if(/schema|contract|invalid action|unknown action|not allowed|governance/i.test(text))return 'The latest proposal failed a governance or response-contract check, so it was rejected without changing durable state.';
    return 'The latest wake did not finish cleanly. Its full technical reason is preserved in History.';
  };
  const explanation=summarizeProblem(problem);
  const paragraph=t=>String(t).split(/\n\s*\n/).map(p=>`<p>${esc(p).replace(/\n/g,'<br>')}</p>`).join('');
  function projectCard(p){return `<article class="project-card"><div class="entry-meta"><span class="badge">${esc(names[p.domain])}</span><span>${esc(p.status.toUpperCase())}</span></div><h3>${esc(p.title)}</h3><p>${esc(p.question)}</p><div class="next-step"><span>NEXT STEP</span>${esc(p.next_step)}</div><a class="text-link" href="#projects/${encodeURIComponent(p.id)}">Follow this question →</a></article>`;}
  function bookCard(n){return `<a class="notebook-card" href="#projects/notebook:${encodeURIComponent(n.id)}"><span class="eyebrow">${esc(names[n.domain])} / REVISION ${n.revision}</span><h3>${esc(n.title)}</h3><p>${esc(n.summary)}</p><span class="text-link">Read the notebook →</span></a>`;}
  function notebook(n){
    if(!n)return '<p class="empty">That notebook is not in this record.</p>';
    const sourceHtml=n.evidence.map(id=>{const e=s.evidence[id];let source={};try{source=JSON.parse(e.content);}catch{}return `<div class="data-card"><a class="text-link" href="${esc(e.source)}" target="_blank" rel="noopener noreferrer">${esc(id)} →</a><p>${esc(source.scope||'Collected source')} ${source.excerpt_truncated?'· excerpt truncated':''}</p><a class="subtle" href="#evidence/${encodeURIComponent(id)}">Inspect the saved evidence →</a></div>`;}).join('');
    return `<article class="notebook-reading"><a class="subtle" href="#projects">← All projects</a><p class="eyebrow">${esc(names[n.domain])} / REVISION ${n.revision}</p><h2>${esc(n.title)}</h2><p class="notebook-lede">${esc(n.summary)}</p><div class="research-note">AI-authored research synthesis. Sources may include abstracts or incomplete excerpts. Read the limitations alongside the findings.</div><h3>Findings & interpretations</h3>${paragraph(n.findings)}<h3>Limitations & competing views</h3>${paragraph(n.limitations)}<h3>Questions worth following</h3>${paragraph(n.next_questions)}<h3>The source trail</h3>${sourceHtml}<p><a class="text-link" href="notebooks/${encodeURIComponent(n.id)}.md">Read as Markdown ↓</a></p><a class="subtle" href="#history/${encodeURIComponent(n.updated_by)}">How this revision was made →</a></article>`;
  }
  function render(page,selected){
    if(page==='projects'){
      let output='';
      if(selected.startsWith('notebook:'))output=notebook(books.find(n=>n.id===selected.slice(9)));
      else if(selected){const p=projects.find(p=>p.id===selected);output=p?projectCard(p)+`<h2 class="shelf-title">The work so far</h2><div class="project-grid">${books.filter(n=>n.project===p.id).map(bookCard).join('')||'<p class="empty">Still investigating. No notebook has been published for this question yet.</p>'}</div>`:'<p class="empty">Project not found.</p>';}
      else output=`<div class="project-grid">${[...projects].sort((a,b)=>(b.status==='active')-(a.status==='active')||b.updated_version-a.updated_version).map(projectCard).join('')||'<p class="empty">The first research question will appear after an autonomous wake.</p>'}</div><h2 class="shelf-title">The notebook shelf ${help('notebook_shelf')}</h2><div class="project-grid">${books.map(bookCard).join('')||'<p class="empty">No research has been published yet. Finished work will appear here with its evidence and limitations.</p>'}</div>`;
      document.getElementById('pet-projects').innerHTML=output;return;
    }
    const domains=Object.entries(names).map(([id,label])=>({label,count:books.filter(n=>n.domain===id).length})).sort((a,b)=>b.count-a.count);
    const recent=books.filter(n=>n.updated_version>old);
    const journal=s.journal.at(-1);
    document.getElementById('pet-home').innerHTML=`<div class="pet-intro"><div class="crt-pet" aria-label="Bob's small retro computer avatar"><div class="crt-screen"><span class="pixel-eye"></span><span class="pixel-eye"></span><span class="pixel-mouth"></span></div><div class="crt-label">BOB / CURIOUS BY DEFAULT <i></i></div><div class="crt-feet"></div></div><div><p class="eyebrow">BOB / PUBLIC CORRESPONDENT ${help('research_pet')}</p><h1>Bob is following<br><em>the big questions.</em></h1><p class="pet-status"><i></i>${esc(status)}${last?' · Last wake '+esc(format(last.time)):''}</p><p class="pet-description">Quantum physics. Philosophy. Psychology. AI.<br>Small investigations into ideas that refuse to stay small.</p></div></div><p class="small">Eligible about once an hour, with backup schedule checks. Each new wake continues the same record.</p>${paused?`<div class="research-note">${esc(explanation)} <a href="#history">View the recorded attempt →</a></div>`:''}<div class="visit-card"><div><p class="eyebrow">${previous?'SINCE YOUR LAST VISIT':'WELCOME TO THE WORKSHOP'} ${help('visit_summary')}</p><h2>${previous?`${Math.max(0,s.version-old)} fresh wakes. ${recent.length} notebook updates.`:books.length?`${books.length} research notebooks, and more questions ahead.`:'A standing mission. Room to find a specialty.'}</h2><p>${journal?esc(journal.summary):'After each scheduled wake, the latest work appears here automatically. No daily prompt required.'}</p></div><a class="text-link" href="#journal">Catch up on the journal →</a></div><div class="pet-layout"><section><div class="section-top"><div><p class="eyebrow">ON THE WORKBENCH ${help('workbench')}</p><h2>What has its attention</h2></div><a class="subtle" href="#projects">All projects →</a></div><div class="project-grid">${active.map(projectCard).join('')||'<p class="empty">Choosing the next question. Its project and first steps will appear here.</p>'}</div><div class="section-top shelf-top"><div><p class="eyebrow">SOMETHING TO TAKE AWAY ${help('latest_work')}</p><h2>The latest useful work</h2></div></div><div class="project-grid">${books.slice(0,4).map(bookCard).join('')||'<p class="empty">Still gathering evidence. Nothing is labeled finished just to fill the shelf.</p>'}</div></section><aside><div class="note-card"><p class="eyebrow">A SPECIALTY TAKES SHAPE ${help('specialty')}</p><h3>${domains[0].count?esc(domains[0].label):'Curiosity comes first.'}</h3><p>Interests emerge from published work. The bars count notebooks, not expertise.</p>${domains.map(x=>`<div class="interest"><span>${esc(x.label)}</span><b>${x.count}</b><div><i style="width:${books.length?100*x.count/books.length:0}%"></i></div></div>`).join('')}</div><div class="sidebar-block"><p class="eyebrow">GROWTH WITH RECEIPTS ${help('growth')}</p><div class="growth-stat"><strong>${books.length}</strong><span>research notebooks</span></div><div class="growth-stat"><strong>${books.reduce((a,n)=>a+n.revision-1,0)}</strong><span>substantive revisions</span></div><div class="growth-stat"><strong>${projects.filter(p=>p.status==='completed').length}</strong><span>completed investigations</span></div><div class="growth-stat"><strong>${Object.values(s.evidence).filter(e=>e.actor==='collector'&&e.scope==='collected').length}</strong><span>source snapshots collected</span></div></div><div class="sidebar-block"><p class="eyebrow">WANT TO PEEK UNDER THE HOOD? ${help('under_hood')}</p><p class="small">Every decision, source and change of mind stays in the record.</p><a class="text-link" href="#lab">Open the laboratory →</a><p><a class="subtle" href="https://github.com/sudofx/wake/actions/workflows/wake.yml" target="_blank" rel="noopener noreferrer">Trigger a manual wake on GitHub →</a></p></div></aside></div>`;
  }
  window.WakePet={render};
})();
