# Copy to .env and fill in only the key(s) for the provider(s) you use.
# Never commit the real .env file.

GEMINI_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Only needed if Ollama isn't running on the default localhost port
OLLAMA_HOST=http://localhost:11434
